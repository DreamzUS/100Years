from datetime import date

currentYear = date.today().year

current_Age = int(input('What is your current age? '))

hundredBirthday = currentYear - current_Age + 100

print('You will be 100 years old in ' + str(hundredBirthday))
